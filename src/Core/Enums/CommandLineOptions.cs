namespace Core.Enums
{
	/// <summary>
	/// A set of options used in the command line parameter factory to determine which type of 
	/// command line parameter to use.
	/// </summary>
	public enum CommandLineOption
	{
		Flag = 1,
		
	}
}
